<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yifan.geekgen.mapper.UserMapper">

    <select id="getById"
            parameterType="long"
            resultType="cn.yifan.geekgen.pojo.entity.User"
    >
        SELECT id, username, password, level, role, school, name, created_at, updated_at, is_deleted FROM user WHERE id = #{id}
    </select>

    <select id="getByUsername"
            parameterType="string"
            resultType="cn.yifan.geekgen.pojo.entity.User"
    >
        SELECT id, username, password, level, role, school, name, created_at, updated_at, is_deleted FROM user WHERE username = #{username}
    </select>

    <insert id="insert"
            parameterType="cn.yifan.geekgen.pojo.entity.User"
            useGeneratedKeys="true"
            keyProperty="id"
    >
        INSERT INTO user (username, password, level, role, school, name)
        VALUES (#{username}, #{password}, #{level}, #{role}, #{school}, #{name})
    </insert>

</mapper>