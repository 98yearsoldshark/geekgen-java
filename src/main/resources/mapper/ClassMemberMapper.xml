<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yifan.geekgen.mapper.ClassMemberMapper">

    <select id="getById"
            parameterType="long"
            resultType="cn.yifan.geekgen.pojo.entity.ClassMember"
    >
        SELECT id, class_id, user_id, role, created_at, updated_at, is_deleted FROM class_member WHERE id = #{id}
    </select>

    <select id="getByClassId"
            parameterType="long"
            resultType="cn.yifan.geekgen.pojo.entity.ClassMember"
    >
        SELECT id, class_id, user_id, role, created_at, updated_at, is_deleted FROM class_member WHERE class_id = #{classId}
    </select>

    <select id="getByUserId"
            parameterType="long"
            resultType="cn.yifan.geekgen.pojo.entity.ClassMember"
    >
        SELECT id, class_id, user_id, role, created_at, updated_at, is_deleted FROM class_member WHERE user_id = #{userId}
    </select>

    <insert id="insert"
            parameterType="cn.yifan.geekgen.pojo.entity.ClassMember"
            useGeneratedKeys="true"
            keyProperty="id"
    >
        INSERT INTO class_member (class_id, user_id, role)
        VALUES (#{classId}, #{userId}, #{role})
    </insert>

</mapper>