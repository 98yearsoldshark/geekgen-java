<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.yifan.geekgen.mapper.TaskProgressMapper">

    <select id="getById"
            parameterType="long"
            resultType="cn.yifan.geekgen.pojo.entity.TaskProgress"
    >
        SELECT id, task_id, task_title, task_type, user_id, user_name, class_name, status, created_at, updated_at, is_deleted FROM task_progress WHERE id = #{id}
    </select>

    <select id="getByTaskId"
            parameterType="long"
            resultType="cn.yifan.geekgen.pojo.entity.TaskProgress"
    >
        SELECT id, task_id, task_title, task_type, user_id, user_name, class_name, status, created_at, updated_at, is_deleted FROM task_progress WHERE task_id = #{taskId}
    </select>

    <select id="getByUserId"
            parameterType="long"
            resultType="cn.yifan.geekgen.pojo.entity.TaskProgress"
    >
        SELECT id, task_id, task_title, task_type, user_id, user_name, class_name, status, created_at, updated_at, is_deleted FROM task_progress WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="getByTaskIdAndUserId"
            parameterType="long"
            resultType="cn.yifan.geekgen.pojo.entity.TaskProgress"
    >
        SELECT id, task_id, task_title, task_type, user_id, user_name, class_name, status, created_at, updated_at, is_deleted FROM task_progress WHERE task_id = #{taskId} AND user_id = #{userId}
    </select>

    <insert id="insert"
            parameterType="cn.yifan.geekgen.pojo.entity.TaskProgress"
            useGeneratedKeys="true"
            keyProperty="id"
    >
        INSERT INTO task_progress (task_id, task_title, task_type, user_id, user_name, class_name, status)
        VALUES (#{taskId}, #{taskTitle}, #{taskType}, #{userId}, #{userName}, #{className}, #{status})
    </insert>

    <update id="update"
            parameterType="cn.yifan.geekgen.pojo.entity.TaskProgress"
    >
        UPDATE task_progress
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="taskTitle != null">
                task_title = #{taskTitle},
            </if>
            <if test="taskType != null">
                task_type = #{taskType},
            </if>
            <if test="userName != null">
                user_name = #{userName},
            </if>
            <if test="className != null">
                class_name = #{className},
            </if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>